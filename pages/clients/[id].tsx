import { Layout } from "../../components/Layout";
import ClientInfo from "./ClientInfo/ClientInfo";

const ClientInfoProxy = () => {
  return <ClientInfo />;
};

ClientInfoProxy.Layout = Layout;
export default ClientInfoProxy;
